[package]
name = "jade"
version = "1.0.7"
edition = "2024"
description = "A languge for LLMs"
license = "MIT"

[dependencies]
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
