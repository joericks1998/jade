// Import a sibling `.jde` file by its module name — `math_lib` loads
// `./math_lib.jde` and binds under `math_lib`. No quotes, no alias.
use math_lib

let x = math_lib.add(2, 3)
let y = math_lib.mul(4, 5)
