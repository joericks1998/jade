interface Displayable {
    fn to_str(self)
}

struct Point {
    x,
    y
}

extend Point: Displayable {
    fn to_str(self) {
        return f"({self.x}, {self.y})"
    }
}

let p = Point { x: 3, y: 4 }
let s = p.to_str()
